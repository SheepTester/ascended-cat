const BlockType = {
  BOOLEAN: 'boolean',
  COMMAND: 'command',
  REPORTER: 'reporter'
}

const ArgumentType = {
  STRING: 'string',
  NUMBER: 'number',
  ANGLE: 'angle',
  BOOLEAN: 'boolean',
  COLOUR: 'colour',
  BRANCH: 'branch'
}

export { BlockType, ArgumentType }
