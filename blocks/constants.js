const BlockType = {
  BOOLEAN: 'boolean',
  COMMAND: 'command',
  REPORTER: 'reporter'
}

const ArgumentType = {
  STRING: 'string',
  NUMBER: 'number',
  ANGLE: 'angle',
  BOOLEAN: 'boolean',
  COLOUR: 'colour',
  BRANCH: 'branch'
}

const NullCategory = 'nonexistent'

export { BlockType, ArgumentType, NullCategory }
